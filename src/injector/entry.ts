// SCAFFOLD PLACEHOLDER. Task 5 owns this file: it becomes the utilityProcess
// entry that owns the hold loop and the native adapters. It is a build entry
// point in electron.vite.config.ts, so it has to exist from Task 1.
export {}

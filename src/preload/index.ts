// SCAFFOLD PLACEHOLDER. Task 10 owns this file and defines the real `window.kpu`
// contextBridge surface against the channels in src/shared/ipc.ts. Nothing is
// exposed yet on purpose: an incomplete bridge is worse than no bridge.
export {}
